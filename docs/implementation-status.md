# 当前交付状态

本仓库从空仓库建立 Shadow Relay 源控制面，包含完整的持久化服务、中文界面、发布契约、配置模板和后续可运行的回归测试。

## 已实现

- Go API/Worker 单二进制、PostgreSQL 迁移与持久任务、独立加密快照目录。
- 协议识别、规范化、结构校验、差异，源审核、固定、回滚和失败隔离。
- 目录同步、候选接纳、忽略、永久屏蔽与指纹去重。
- 编排成员、过滤、主备优先级，独立 Provider 快照与传统聚合导出。
- 不可变版本、稳定指针、回滚、客户端格式权限与令牌生命周期。
- 外部运行时服务检测、状态读取、能力连接和原生后台入口。
- SSRF 出站边界、凭据加密、审核审计和脱敏反馈。
- 中文响应式管理页面、导入预览、编辑、版本详情、发布预览、QR 绑定。
- Docker/Compose 配方、开发入口、OpenAPI、JSON Schema 与设计/安全文档。

## 执行边界

本轮根据原始沟通新增：原生 Hub 插件转换/脚手架、relay-bridge、四阶段 live smoke、fixture 采集工具、自动发布门槛、独立体检周期、yuanc 目录、净化规则、新旧漫画仓、直播频道覆盖与 TXT/EPG 导出、播客制作、阅读深链、参考覆盖目录和本地内容版本更新。

跨源小说执行交给官方 Hub。其余连接器仍以拉取状态为主；只有显式 Hub 热加载使用管理写接口。未实现或宣称完整阅读 JS 引擎、自动安装漫画扩展、跨媒体全文搜索、视频代理、Shadow Media Android 托管模式或听读联动。完整参考项目去向见 `docs/reference-audit.md`，运行链路见 `docs/hub-bridge.md`。没有进行服务部署或 APK 构建。

## 验证记录

实现过程中已完成过后端编译、Go vet、带竞态检查的真实 PostgreSQL 回归，以及前端构建和 API 单元测试。浏览器流程已执行到源导入、审核、启用、编排和发布；绑定表单测试暴露了标签定位问题，已改为显式 label/id 和 role 定位。

2026-09-02 的提交保留独立源快照、相对链接规范化、脱敏反馈等后续改动，当时最终提交尚未经过完整回归与浏览器复测。新增测试已经写入仓库，CI 设置为手动触发或 PR 触发，普通推送不自动运行测试。

2026-09-02 补全原始沟通后的代码继续遵循“先不急着测试”：仅做代码静态检查、gofmt、Prettier 格式整理和提交差异检查；未运行任何新增测试、编译、实网 smoke 或容器构建。新增转换安全边界、插件归属保护、目录/漫画格式及频道导出回归用例均未执行。格式工具暴露的语法问题已修正，不能据此宣称运行验收通过。

Docker 配方尚未在本机实际构建；浏览器流程未复测。


### 2026-09-03：按源代理与书源 API 验证

- 新增服务器命名 HTTP(S) CONNECT 代理配置、管理员代理 ID 列表和源 `proxyId`，覆盖预览、导入、同步及 Relay 内容抽样。未配置的源继续直连，代理失败不自动直连。
- 新增源 `hubProxyMode` 及 recipe `proxyMode`，转换报告、ZIP、发布清单包含代理策略，策略变化更新插件版本和自动发布输入签名。Hub 地址和凭据只由 Hub 自身保存。
- 新增真实 HTTP 隧道与 PostgreSQL API 回归，覆盖认证、配置脱敏、直连/代理隔离、代理拒绝、目标 DNS 检查、重定向拦截、HTTPS Host/SNI/证书校验、源编辑保留配置和插件版本更新。
- `go test -race ./...`（独立 PostgreSQL schema）、`go vet ./...`、前端构建通过；回归发现并修正既有漫画仓 fixture 的过时期望（仓库元信息本身也形成一个目录条目）。本次未打开页面或运行浏览器测试。
- 在 NAS 临时 API 实例以独立 schema 调用现有书源数据，验证转换、导入、审核、启用、发布及代理插件 ZIP；原文未离开 NAS，临时进程、schema 和数据已清理，生产服务未切换。
- 真实数据存在原始快照 URL 校验失败，以及 JavaScript、链式/位置选择器、认证和复杂请求选项等不兼容规则。结构导入、样例发布成功不等于真实书站可读；现有环境没有 Hub 运行时，也未配置真实出口代理，搜索/详情/目录/正文与实际代理出口未验收。详细盘点和接口统计保留在仓库之外的本地运维记录。


### 2026-09-03：真实书源发布深入验证

按用户要求使用 NAS 内现存书源、已存储版本和加密快照，在独立 schema 和临时 API 中验证，不导出原文。覆盖逐包审核/启用/发布、下载规则逐项一致性、多个真实包合并去重、发布预览无副作用、真实规则副本的待审/拒绝/批准/再次发布、固定版本、两层回滚、健康与媒体过滤、隔离释放、绑定权限/跨组隔离/轮换/吊销、条件请求，以及真实订阅上游同步和自动发布。33 个书源的队列体检仅为结构级，没有 Hub 时不声称完成搜索/详情/目录/正文实网验收。实际源清单、失败类别、计数和同步结果记录在仓库外运维文档。

修复两项编译问题：Hub 不兼容规则不再占用 500 个插件上限或进入安装清单；阅读规则导出正确使用经过语言/地区过滤的条目。增加只编译不保存的发布预览、具体源校验错误、排除原因和格式兼容警告，并补齐大书源包、过滤与失败保持旧发布的脱敏回归。原始规则 URL 校验失败继续明确拒绝，不自动跳过或改写规则。完整 Go/PostgreSQL 竞态回归和 go vet 通过，生产服务未切换；此次未打开页面。
